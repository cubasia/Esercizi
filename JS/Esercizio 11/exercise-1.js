const sum = function (a, b) {
  return a + b;
}

const log = function (value) {
  console.log(value);
}

log(sum(2, 5));

const sum2 = (a, b)=>{return a + b}

const log2 = (a) => console.log(a)

log2(sum2(2,5))